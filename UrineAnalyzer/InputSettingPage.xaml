<Page x:Class="UrineAnalyzer.InputSettingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UrineAnalyzer"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="1000"
      Title="InputSettingPage" FontSize="16" Loaded="Page_Loaded">

    <Canvas Background="White" Margin="5,5,5,5">
        <GroupBox Header="设置默认输入项目" Height="545" Width="582" Canvas.Left="38" Canvas.Top="47">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>

                <DataGrid Name="dgInputDefault" Margin="2,2,2,2" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" BorderBrush="LightGray" VerticalGridLinesBrush="LightGray" HorizontalGridLinesBrush="LightGray" CellEditEnding="dgInputDefault_CellEditEnding">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="项目类别" Width="150" Binding="{Binding TypeName}" IsReadOnly="True"/>
                        <DataGridTextColumn Header="默认输入值" Width="*" Binding="{Binding Caption}"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Column="1">
                    <Button Name="btnSave" Content="保存" Style="{StaticResource DialogButton}" Margin="5,15,5,5" Click="btnSave_Click"/>
                    <Button Name="btnUnSave" Content="取消保存" Style="{StaticResource DialogButton}" Margin="5,5,5,5" Click="btnUnSave_Click"/>
                </StackPanel>
            </Grid>
        </GroupBox>
    </Canvas>
</Page>
