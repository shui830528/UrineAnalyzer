<Page x:Class="UrineAnalyzer.HospInfoSettingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UrineAnalyzer"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="1000"
      Title="HospInfoSettingPage" FontSize="16" Loaded="Page_Loaded">

    <Canvas Background="White" Margin="5,5,5,5">
        <GroupBox Header="信息类别" Height="450" Width="215" Canvas.Left="20" Canvas.Top="20">
            <ListBox Name="lbHospInfoType" Margin="2,2,2,2" DisplayMemberPath="Caption" SelectionChanged="lbHospInfoType_SelectionChanged"/>
        </GroupBox>
        <GroupBox Name="gbHospInfo" Header="送检医生" Height="450" Width="376" Canvas.Left="250" Canvas.Top="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>

                <DataGrid Name="dgHospInfo" CanUserAddRows="False" CanUserDeleteRows="False" AutoGenerateColumns="False" BorderBrush="LightGray" HorizontalGridLinesBrush="LightGray" VerticalGridLinesBrush="LightGray" IsReadOnly="True" CellEditEnding="dgHospInfo_CellEditEnding">
                    <DataGrid.Columns>
                        <DataGridTextColumn x:Name="columnName" Header="送检医生" Width="*" Binding="{Binding Caption}"/>
                        <DataGridTextColumn x:Name="columnCode" Header="助记符" Width="120" Binding="{Binding Code}"/>
                    </DataGrid.Columns>
                </DataGrid>
                <StackPanel Grid.Column="1">
                    <Button Name="btnAdd" Content="添加" Style="{StaticResource DialogButton}" Height="30" Margin="5,15,5,5" Click="btnAdd_Click"/>
                    <Button Name="btnDelete" Content="删除" Style="{StaticResource DialogButton}" Height="30"  Margin="5,5,5,5" Click="btnDelete_Click"/>
                    <Button Name="btnSave" Content="保存" Style="{StaticResource DialogButton}" Height="30"  Margin="5,5,5,5" Click="btnSave_Click"/>
                    <Button Name="btnUnSave" Content="取消保存" Style="{StaticResource DialogButton}" Height="30"  Margin="5,5,5,5" Click="btnUnSave_Click"/>
                </StackPanel>
            </Grid>
        </GroupBox>
    </Canvas>
</Page>
