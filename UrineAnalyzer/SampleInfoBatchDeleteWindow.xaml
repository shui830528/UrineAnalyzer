<local:ModernWindow x:Class="UrineAnalyzer.SampleInfoBatchDeleteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Style="{StaticResource MetroWindowStyle}"
        xmlns:local="clr-namespace:UrineAnalyzer"
        mc:Ignorable="d"
        Title="样本批量删除" Height="300" Width="450" ResizeMode="NoResize" ShowInTaskbar="False" FontSize="16" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>

        <TextBlock Text="注意：已审核样本不能删除" Margin="2,2,2,2"/>
        
        <Grid Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
                
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="130"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="样本号" VerticalAlignment="Center" Margin="5,0,5,0"/>
            <TextBox Name="tbStartSampleNo" Grid.Column="1" Text="{Binding Path=SampleID}" Width="130" VerticalAlignment="Center"/>
            <TextBlock Grid.Column="2" Text=" - " VerticalAlignment="Center"/>
            <TextBox Name="tbEndSampleNo" Grid.Column="3" Text="{Binding Path=SampleID}" Width="130" VerticalAlignment="Center"/>
            
            <TextBlock Grid.Row="1" Text="登记日期" VerticalAlignment="Center" Margin="5,0,5,0"/>
            <DatePicker Name="dpStartDate" SelectedDate="{Binding Path=RegistDate}" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center"/>
            <TextBlock Grid.Row="1" Grid.Column="2" Text=" - " VerticalAlignment="Center"/>
            <DatePicker Name="dpEndDate" SelectedDate="{Binding Path=RegistDate}" Grid.Row="1" Grid.Column="3" VerticalAlignment="Center"/>
        </Grid>

        <Border Grid.Row="2" BorderThickness="1" BorderBrush="LightGray">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Name="btnOk" Content="确定" Style="{StaticResource DialogButton}" Height="30" Width="100" Click="btnOk_Click"/>
                <Button Name="btnCancel" Content="取消" Style="{StaticResource DialogButton}" Height="30" Width="100" Margin="50,0,0,0" Click="btnCancel_Click"/>
            </StackPanel>
        </Border>
        
    </Grid>
</local:ModernWindow>
