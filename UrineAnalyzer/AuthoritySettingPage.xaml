<Page x:Class="UrineAnalyzer.AuthoritySettingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UrineAnalyzer"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="1000"
      Title="AuthoritySettingPage" FontSize="16" x:Name="authority" Loaded="authority_Loaded">
    <Canvas Background="White" Margin="5,5,5,5">
        <GroupBox Header="角色" Canvas.Left="30" Canvas.Top="33" Height="350" Width="160">
            <ListBox Name="authoritys" Margin="2,2,2,2" SelectionChanged="authoritys_SelectionChanged"/>
        </GroupBox>

        <GroupBox Header="权限设置" Height="350" Width="437" Canvas.Left="195" Canvas.Top="33">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.2*"/>
                    <ColumnDefinition/>

                </Grid.ColumnDefinitions>

                <GroupBox Header="功能模块">
                    <ListBox Name="module" SelectionChanged="module_SelectionChanged"/>
                    <!--<DataGrid x:Name="module" FrozenColumnCount="1" Margin="5,5,5,5" AutoGenerateColumns="False" CanUserAddRows="False" GridLinesVisibility="None" Background="White" HeadersVisibility="None">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="选择" Width="20">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsChecked,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left" VerticalAlignment="Center" IsEnabled="True" Margin="5,5,0,5"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="功能" Width="*" Binding="{Binding Name}" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>-->
                </GroupBox>
                <GroupBox Header="权限" Grid.Column="1" >
                    <DataGrid x:Name="function" FrozenColumnCount="1" Margin="5,5,5,5" AutoGenerateColumns="False" CanUserAddRows="False" GridLinesVisibility="None" Background="White" HeadersVisibility="None">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="功能" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsChecked,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Content="{Binding FunctionName}" HorizontalAlignment="Left" VerticalAlignment="Center" IsEnabled="True" Margin="5,0,0,0"/>
                                        <!--<CheckBox IsChecked="{Binding IsChecked}" Content="{Binding FunctionName}" HorizontalAlignment="Left" VerticalAlignment="Center" IsEnabled="True" Margin="5,0,0,0"/>-->
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </GroupBox>
            </Grid>
        </GroupBox>
        <Button Name="button_Save" Content="保存" Style="{StaticResource DialogButton}" Height="30" Width ="100" Canvas.Left="532" Canvas.Top="388" Click="button_Save_Click"/>
    </Canvas>
</Page>
