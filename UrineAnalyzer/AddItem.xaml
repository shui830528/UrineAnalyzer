<local:ModernWindow x:Class="UrineAnalyzer.AddItem"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Style="{StaticResource MetroWindowStyle}"
        xmlns:local="clr-namespace:UrineAnalyzer"
        mc:Ignorable="d"
        Title="项目" Height="710" Width="660" FontSize="16" UseLayoutRounding="True" Loaded="Window_Loaded" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" >
    <Canvas>
        <DataGrid x:Name="dgItemInfo" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" BorderBrush="LightGray" VerticalGridLinesBrush="LightGray" HorizontalGridLinesBrush="LightGray" Height="590" Canvas.Left="12" Canvas.Top="10" Width="610">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="选择" Width="45">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsChecked,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="检测项目" Binding="{Binding Name}" IsReadOnly="True"/>
                <DataGridTextColumn Header="项目名称" Binding="{Binding Caption}" IsReadOnly="True"/>
                <DataGridTextColumn Header="单位" Width="80" Binding="{Binding Unit}" IsReadOnly="True"/>
                <DataGridComboBoxColumn  Width="160" Header="结果">
                    <DataGridComboBoxColumn.EditingElementStyle>
                        <Style TargetType="ComboBox">
                            <Setter Property="SelectedValue" Value="{Binding Path=Result}" />
                            <Setter Property="ItemsSource" Value="{Binding Path=ResultList}" />
                        </Style>
                    </DataGridComboBoxColumn.EditingElementStyle>
                    <DataGridComboBoxColumn.ElementStyle>
                        <Style TargetType="ComboBox">
                            <Setter Property="SelectedValue" Value="{Binding Path=Result}" />
                            <Setter Property="ItemsSource" Value="{Binding Path=ResultList}" />
                        </Style>
                    </DataGridComboBoxColumn.ElementStyle>
                </DataGridComboBoxColumn>
                <DataGridTextColumn Header="参考范围" Width="160" Binding="{Binding Range}" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="btnOk" Content="确定" Style="{StaticResource DialogButton}" Height="30" Click="btnOk_Click" Canvas.Left="357" Canvas.Top="615" Width="110"/>
        <Button x:Name="btnCancel" Content="取消" Style="{StaticResource DialogButton}" Height="30" Click="btnCancel_Click" Canvas.Left="512" Canvas.Top="615" Width="110"/>

    </Canvas>
</local:ModernWindow>
